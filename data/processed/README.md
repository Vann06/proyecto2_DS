# Datos procesados

Esta carpeta se reservará para datasets analíticos finales y características
derivadas en notebooks posteriores. Los archivos generados se ignoran en Git
y deben poder reproducirse a partir de `data/raw/` y `data/interim/`.
