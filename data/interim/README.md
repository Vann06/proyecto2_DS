# Datos intermedios

Esta carpeta contiene copias derivadas de `data/raw/` después de controles y
limpieza reproducible. El notebook `01_data_quality.ipynb` genera:

- `summaries_train_clean.csv`
- `prompts_train_clean.csv`

Los CSV se ignoran en Git; este README documenta cómo volver a crearlos.
